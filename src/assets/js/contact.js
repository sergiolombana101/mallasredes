/*if(localStorage.getItem("loaded") == "true" && localStorage.getItem("component") == "contact"){
    let inputs = document.getElementsByClassName("input");

    for(let x = 0; x<inputs.length;x++){
        inputs[x].onfocus = () => {
            console.log(inputs[x].name+" on focus");
            let label = document.getElementById(inputs[x].name+"-label");
            console.log(label);
            label.style.transition = "1s";
            label.style.top = "7%";
            label.style.fontSize = ".9vw";
            label.style.color = "white";
        }

    }
}*/